# iCESugar UART Echo

This example receives UART bytes and sends them back.

Default parameters:

- Clock: 12 MHz
- Baud rate: 115200
- UART TX pin: board package `uart_tx`
- UART RX pin: board package `uart_rx`

This is intended as a professional project template. Check your USB-UART wiring before programming.
