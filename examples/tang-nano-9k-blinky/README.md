# Tang Nano 9K LED Blinky

这是 Gowin/Himbaechel 流程验收例程，目标板卡为 Tang Nano 9K。

推荐流程：

1. 在例程工作台创建并打开本例程。
2. 进入一键构建，运行检查、综合、布局布线、生成比特流。
3. 在报告中心查看 `output/bitstream/top.fs` 和 nextpnr 报告。

约束仅覆盖基础 clock 和 LED，用于快速构建验证。
