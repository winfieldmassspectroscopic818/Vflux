# iCESugar PWM Breathing LED

This example generates a breathing LED effect with a simple PWM ramp.

Expected result:

- `led1` changes brightness slowly.
- `led2` and `led3` show extra counter activity.

Use it to test synthesis, place-and-route, bitstream generation, and iCESugar drag-and-drop programming.
