# iCESugar Button Debounce

This example shows a basic FPGA input workflow:

- Synchronize an external button input.
- Debounce it with a counter.
- Toggle `led1` on each clean button press.

`led2` mirrors the debounced button state and `led3` blinks as a heartbeat.
